# Harsha-K
“Built an end-to-end ETL pipeline using Databricks, PySpark, and SQL to ingest, clean, and transform large datasets. Implemented Medallion Architecture (Bronze, Silver, Gold) with Delta tables for reliable storage and created optimized data models for analytics and reporting.”
